{
  "builds": [
    {
      "src": "client/**",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "client/dist"
      }
    }
  ],
  "ignoreCommand": "git diff HEAD^ HEAD --quiet server/"
}
