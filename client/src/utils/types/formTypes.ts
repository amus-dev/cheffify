export interface LoginFormType {
  email: string;
  password: string;
}

export interface AccountFormType {
  email: string;
  name: string;
  lastName: string;
  phone: number;
  password: string;
  confirmPassword: string;
}
